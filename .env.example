# Python: contoh database eksperimen lokal. Untuk membaca Neon, ganti dengan
# postgresql://... dan ekspor DATABASE_URL sebelum menjalankan notebook/script.
# make_engine() membatasi koneksi PostgreSQL menjadi READ ONLY.
# Aplikasi Next.js memakai web/.env.local; lihat web/.env.example.
DATABASE_URL=sqlite:///data/coffee.db
