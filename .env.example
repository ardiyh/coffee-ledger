# Salin file ini ke `.env` (jangan commit `.env`).
# Dev lokal default-nya SQLite, jadi ini boleh dibiarkan apa adanya dulu.
# Nanti di Fase 3 (deploy), DATABASE_URL diganti ke Postgres (Neon/Supabase).
DATABASE_URL=sqlite:///data/coffee.db
