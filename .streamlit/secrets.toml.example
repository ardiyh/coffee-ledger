# CONTOH secrets — JANGAN taruh yang asli di sini / di git.
# Lokal: gak perlu file ini (default-nya SQLite).
# Di Streamlit Cloud: isi lewat menu "Secrets" (Settings → Secrets), bukan commit file.
DATABASE_URL = "postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require"
